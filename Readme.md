##if repo not exist in github
gh repo create quickstart

##To run this repo locally
git clone --recurse-submodule git@github.com:mustafaerbay/quickstart.git

##and
hugo server --bind "192.168.1.30" -D
